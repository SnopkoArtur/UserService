spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/userservice
    username: postgres
    password: password
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    show-sql: true